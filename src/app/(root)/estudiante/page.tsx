import { signOut } from "@/auth.config";
import { RegistroForm } from "@/components/estudiante/RegistroForm";

export default function StudentPage() {

  return (
    <div>
      <RegistroForm />
    </div>
  );
}